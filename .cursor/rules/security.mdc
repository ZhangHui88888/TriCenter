---
description: "安全约束规则，所有代码修改时自动检查"
alwaysApply: true
---

# 安全约束

## 禁止提交的内容
- API 密钥、Secret 等凭据
- .env 文件中的真实密码
- 第三方服务（AI API、支付）的密钥真实值
- 用户隐私数据（测试数据中不含真实用户信息）

## 数据安全
- 用户敏感信息（密码、支付信息）必须加密存储
- 使用安全存储方案存储 token 和敏感凭据
- 不在日志中输出用户隐私信息

## API 安全
- 所有 API 请求必须携带认证 token
- 敏感接口（支付、用户数据）需要额外校验
- 关键操作必须有防重放机制

## 前端安全
- 用户输入必须校验和清理
- 敏感操作（支付、删除）需要二次确认
- Token 过期必须跳转登录页
