---
description: "测试规范，编写测试时自动应用"
alwaysApply: false
---

# 测试规范

## 基本要求
- 每个 API 开发完成后必须进行功能测试
- 测试应覆盖正常流程和异常情况
- 测试结果按模块记录在 `docs/test/` 目录下对应文件

## 测试覆盖范围

每个 API 应测试以下场景（适用的）：

- **正常场景** — 正常请求和响应、分页/排序/过滤等查询参数
- **参数校验** — 空值、超长值、特殊字符、SQL 注入、负数和零值、无效枚举值
- **权限校验** — 无 Token、无效/过期 Token、权限不足
- **业务异常** — 资源不存在、状态冲突、并发场景

## 测试数据
- 使用 setup/teardown 方法准备和清理测试数据
- 避免测试间数据污染
- 避免依赖外部服务，使用 Mock

## 测试文档规范

每个 API 的测试记录必须包含：
1. **认证要求** — 标明是公开、需认证还是需管理员
2. **前置条件** — 说明需要先调用哪些 API
3. **请求格式** — 完整的请求路径、Header、Body 格式
4. **响应格式** — 预期的响应 JSON 结构
5. **测试用例表** — 测试场景、前置条件、请求入参、预期响应、实际结果、状态

## 注意事项
- 测试环境使用独立的数据库配置
- 中文字符在脚本断言中可能有编码问题，建议断言用英文值
- 脚本文件确保保存为 UTF-8 编码
