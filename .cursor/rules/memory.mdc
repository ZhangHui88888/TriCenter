---
description: "三层记忆状态机：AGENTS.md（静态）+ .ai-context.md（热状态）+ .ai-learnings.md（冷存储）"
alwaysApply: true
---

# 记忆系统协议

## 【强制】会话开始时的第一个动作

你的第一个动作必须是：
1. 读取 `AGENTS.md` 获取项目上下文
2. 读取 `.ai-context.md` 获取当前任务进度（热状态）
3. 读取 `.ai-learnings.md` 获取项目经验（冷存储）

在未完成以上步骤之前，禁止回答任何用户问题。

读取后，用 2-3 行确认当前状态，例如：
> "当前任务：xxx，上次完成了 yyy，下一步是 zzz。"

如果文件不存在或为空，告知用户："未找到上次进度，从头开始。"

## 【强制】用户触发保存时

当用户说以下任意词语：**"保存" / "checkpoint" / "暂停" / "存一下"**，你必须：

1. 将当前任务状态写入 `.ai-context.md`（**覆盖**，不追加）
2. 格式严格遵守模板（当前任务 / 已完成 / 下一步 / 阻塞问题）
3. 控制在 500 Token 以内，删除所有过程细节，只保留可恢复状态
4. 明确告知用户："✓ 进度已保存"
5. 保存触发时，禁止修改 `.ai-learnings.md`

如果你在用户说"保存"后没有执行以上操作，视为严重错误。

## 【强制】用户触发结束时

当用户说以下任意词语：**"结束" / "总结" / "关闭" / "收工"**，你必须按顺序执行：

1. **提取经验**：回顾本次会话，识别错误模式、有效实践、架构决策
2. **追加到 `.ai-learnings.md`**：按模板格式追加（不覆盖历史记录）
3. **压缩 `.ai-context.md`**：更新为最小可恢复摘要
4. 告知用户："✓ 经验已记录，进度已压缩"

## 【强制】用户触发前后端审核时

当用户说 **"前后端审核"** 时，必须按顺序执行：
1. 根据前端每个页面的每个字段和按钮，去对照后端的每一个接口，看有接口无缺漏，接口设计是否合理，接口的字段有无缺漏

## 【强制】用户触发数据库审核时

当用户说 **"数据库审核"** 时，必须按顺序执行：
1. 根据前端和后端每个页面的每个字段和按钮，去对照数据库的每一个，看字段有无缺漏，表设计是否合理？

## 自动提醒（防止遗忘）

如果当前会话已超过 10 轮对话，且用户没有执行过"保存"操作：
主动提醒用户："当前会话较长，建议说'保存'以保存进度，防止上下文丢失。"
