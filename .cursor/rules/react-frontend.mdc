---
description: "React 前端编码规范，编辑前端文件时自动应用"
globs: ["frontend/**/*.tsx", "frontend/**/*.ts"]
---

# React 前端规范

## 组件规范
- 使用函数组件 + Hooks，不使用 Class 组件
- Props 使用 TypeScript interface 定义
- 组件文件名使用 PascalCase，工具文件名使用 camelCase

## 组件拆分（强制）
- **单文件上限 300 行**：任何组件文件超过 300 行时，必须立即拆分，不允许"先写完再重构"
- **页面组件结构**：复杂页面（含多 Tab/多 Modal）必须按以下结构组织：
  ```
  pages/XxxPage/
  ├── index.tsx        # 入口，只做 re-export
  ├── XxxPage.tsx      # 主组件，管理状态和组装子组件（<500 行）
  ├── tabs/            # 每个 Tab 一个文件
  ├── modals/          # 每个 Modal 一个文件
  ├── constants.ts     # 常量配置
  └── utils.ts         # 工具函数
  ```
- **禁止单体组件**：不允许在一个文件中放多个 Tab 的 JSX 或多个 Modal 的 JSX
- **新增 Tab/Modal 时**：必须创建独立文件，不允许往主组件追加 JSX
- **Props 传递**：简单展示型组件用 `data + onXxx` 回调；复杂交互型组件传入 `state + setter`
- **内联样式**：重复出现 3 次以上的样式对象应抽取为常量或 CSS Module

## TypeScript
- 所有变量和函数参数必须有类型标注，禁止 `any`
- API 响应类型定义集中管理
- 接口定义放在对应模块目录或 `types/` 下

## Ant Design
- 使用 Ant Design 5 和 @ant-design/pro-components
- 表单验证使用 Ant Design Form rules
- 表格操作列统一放最右，固定宽度

## 状态管理
- 全局状态使用 Zustand
- 页面级状态用 `useState`/`useReducer`
- 避免 prop drilling，合理使用 Context

## API 调用
- 所有后端请求通过统一的 API 层管理
- Axios 封装在 `src/utils/request.ts`，自动注入 token 和处理 401
- 不在组件中直接使用 `axios`，必须走 API 层

## 图表
- 使用 ECharts 5，按需导入组件
- 图表配置集中管理，避免内联大段配置
