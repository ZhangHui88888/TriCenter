# 当前任务上下文

## 当前任务
统一行业分类与产品品类为单一分类体系（categories 表）。

## 已完成的步骤
- **后端**：新建 `Category` 实体 + `CategoryMapper`，替换全部 `IndustryCategory`/`ProductCategory` 引用
- **后端**：`DictionaryCacheService`、`OptionsService/Impl`、`OptionsController`、`TreeCategoryServiceImpl`、`EnterpriseServiceImpl`、`DashboardServiceImpl`、`ProductServiceImpl`、`SurveyExcelServiceImpl` 等全部改用统一 Category
- **后端**：删除旧 `IndustryCategory.java`、`ProductCategory.java`、`IndustryCategoryMapper.java`、`ProductCategoryMapper.java`
- **后端**：`mvn compile` 通过
- **数据库**：`schema.sql` DDL 合并两表为 `categories`，外键已更新，TRUNCATE 已合并，行业种子数据已改为 `INSERT INTO categories`
- **数据库**：产品品类种子数据（ID 与行业分类重叠）标记为【待审核】，用户需决定去重方案
- **前端**：`api.ts` 合并 `getIndustries`/`getProductCategories` → `getCategories`
- **前端**：`utils.ts` 统一工具函数（`mapCategoriesToCascader`、`findCategoryPath`、`findCascaderPath`），旧名保留为别名
- **前端**：`useEnterpriseData.ts` 合并两次分类加载为一次 `getCategories()`
- **前端**：`EnterpriseList.tsx` 合并两次 API 调用为一次
- **前端**：`EditEnterpriseModal.tsx` 移除重复的 `findIndustryCascaderPath`，改用 utils 导入
- **前端**：`DataAnalysis.tsx` 更新 API 调用
- **前端**：`DataDictionary.tsx` 合并行业/产品分类管理为"分类管理"
- **前端**：`tsc --noEmit` 通过
- **文档**：`docs/README.md`、`docs/tech/数据库设计文档.md` 已更新

## 下一步计划
1. 用户审核 `schema.sql` 中产品品类种子数据的去重方案
2. 生产环境数据库迁移脚本（合并两表 + 数据迁移）

## 阻塞问题
- 产品品类种子数据 ID 与行业分类 ID 重叠，需用户审核决定保留/去重/偏移方案。
