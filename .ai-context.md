# 当前任务上下文

## 当前任务
1. **模块引导**：除登录外全站页面补指引（企业详情已接好；若还有孤立路由再扫）。
2. **原主线（可选并行）**：企业详情已知 P0、验收企业样本、文档端口/API 对账等。

## 已完成
- **企业详情对齐（历史）**：详情 Tab/弹窗与 PUT；验收清单与 fixtures；本地 API+DB 样例勾选。
- **顶栏模块引导**：`AppGuide` + 配置；仅用户选模块触发；企业详情三步 + `data-tour`。
- **下线独立漏斗分析**：前后端与文档已收敛到看板/数据分析（见历史提交）。
- **本地 Sonar 管线**：`pom.xml` sonar 插件与属性；`run-sonar.ps1`（读 `.sonar-token.local`）；`src/test/java` 占位；`docs/tech/SonarQube-Maven终端分析.md`；Docker Desktop + `sonarqube:community`；已成功 `mvn sonar:sonar`（仪表盘 `com.tricenter:tricenter-backend`）。Sonar BLOCKER（`DataInitializer` 默认口令）**开会演示期内暂不修改**。
- **开发计划澄清**：`docs/plan/开发计划.md`「代码质量与死代码治理」条下注明：按模块=去死代码/静态清理粒度，≠ 企业详情前端模块化，≠ 默认含后端 S3776 大拆；后者需另列任务。

## 下一步
- 继续：孤立路由与引导、企业详情 P0、验收 id 清理、文档对账。
- 按需：Sonar 大批量规则（如 S1192）分期治理；勿与大会演示默认账号冲突。

## 阻塞问题
- 暂无。
