# 当前任务上下文

## 当前任务
企业详情前后端字段对齐、验收文档与数据库全字段核对流程固化。

## 已完成
- 企业详情页：基本信息展示/编辑对齐（跨境营收展示、行业 Cascader 路径、联系人展示邮箱微信备注）；产品弹窗改为字典 ID + 出口/利润落库；自主品牌接 PUT；专利去掉无库字段「类型」；跟进弹窗去掉无后端字段「合作服务商」；产品概览分组说明与进出口展示与弹窗一致。
- 后端：`ProductCreate/UpdateRequest` 与 `ProductResponse` 增加 `exportRatio`、`profitMargin`，`ProductServiceImpl` 更新逻辑已接。
- 文档：`docs/test/企业全字段数据库验收清单.md`（分类字段表 + 创建/全量编辑双列勾选）及 `docs/test/fixtures/` 下 JSON 样例；`docs/README.md` 已登记。
- 本地已用 API Key 跑通：企业 id=4 全量 PUT + 联系人/产品/专利；id=5 仅创建 + 联系人；MySQL 核对与清单勾选一致。

## 下一步
- 按需清理库内验收企业 id=4/5 或保留作回归样本。
- 前端仍存已知问题（未在本次改）：外贸/跨境顶栏 Switch 仅本地态、外贸业绩编辑弹窗未 PUT、竞争对手弹窗假保存、风险区写死示例与 `risks` 字段后端未接等——见此前「严格顺序」审计结论，可按 P0 排期修。
- 若需：把验收清单与 `模块3-企业管理` 测试文档互链或补端口说明（README 仍写 8080，实际 TriCenter 为 8081）。

## 阻塞问题
- 暂无。
