# 当前任务上下文

## 当前任务
企业来源新增"服务商"选项及二级分类（智慧通、常州冠捷）。

## 已完成的步骤
- `tricenter_schema.sql`：enterprises 表新增 `source_provider_id` 字段 + FK；source 选项新增"服务商"；新增 `source_provider` 类别（智慧通、常州冠捷）；附带生产升级 ALTER 注释。
- `tricenter_init.sql`：同步新增 source/source_provider 初始数据。
- `数据库设计文档.md`：enterprises 表补充 `source_provider_id` 字段说明；system_options category 列表补充 `source_provider`。
- 后端：Enterprise entity、EnterpriseDetailResponse、EnterpriseCreateRequest、EnterpriseUpdateRequest 均新增 `sourceProviderId`；EnterpriseServiceImpl 在 create/update/detail 中处理该字段及 label 解析。
- 后端：SurveyExcelServiceImpl 导出时将来源显示为"服务商 - 智慧通"格式。
- 前端：`constants.ts` ENTERPRISE_SOURCES 新增"服务商"。
- 前端：`EditEnterpriseModal` 新增 `sourceProviderOptions` prop，选择"服务商"时联动显示二级下拉，切换来源时自动清空子分类。
- 前端：`EnterpriseDetail.original.tsx` 新增 `sourceProviderOptions` 状态，fetch `source_provider` 字典，传递给 EditEnterpriseModal，detail 映射补充 `source_provider/source_provider_id`。
- 前端：`BasicInfoTab` 企业来源显示"服务商 - 智慧通"格式。
- 后端 `mvn -q -DskipTests compile` 通过。

## 下一步计划
1. 生产环境部署时执行 ALTER 脚本添加 `source_provider_id` 列 + 插入 source_provider 字典数据。
2. 可选：EnterpriseList 高级筛选增加 source_provider 子筛选；Excel 导入支持 source_provider 列。

## 阻塞问题
- 暂无。
