       # 模块6：看板统计 API测试文档

> 测试日期：2026-02-03
> 测试人员：AI Assistant
> 测试状态：✅ 全部通过

---

## 概述

本模块包含5个API，用于首页Dashboard数据展示：
- 6.1 统计概览
- 6.2 漏斗阶段分布
- 6.3 区域分布
- 6.4 行业分布
- 6.5 待跟进提醒

---

## 前置条件

### 登录获取Token

**请求：**
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "user": { "id": 1, "username": "admin", "role": "admin", "name": "系统管理员" }
  }
}
```

---

## 6.1 统计概览

### 基本信息
| 项 | 值 |
|-----|-----|
| 路径 | `GET /api/dashboard/overview` |
| 认证 | 🔒 需要Token |
| 说明 | 获取首页统计卡片数据 |

### 请求格式
```http
GET /api/dashboard/overview
Authorization: Bearer <token>
```

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "totalEnterprises": 5,
    "potentialCount": 5,
    "hasDemandCount": 0,
    "signedSettledCount": 0,
    "monthlyChange": {
      "total": 5,
      "potential": 5,
      "hasDemand": 0,
      "signedSettled": 0
    }
  }
}
```

### 测试用例

| 测试场景 | 请求 | 预期响应 | 实际结果 | 状态 |
|---------|------|---------|---------|------|
| 正常获取概览 | GET /api/dashboard/overview | code=200, 返回统计数据 | 返回企业总数5，潜在5 | ✅ |
| 无Token访问 | 不带Authorization | code=401 | 返回401 | ✅ |

---

## 6.2 漏斗阶段分布

### 基本信息
| 项 | 值 |
|-----|-----|
| 路径 | `GET /api/dashboard/funnel` |
| 认证 | 🔒 需要Token |
| 说明 | 获取漏斗各阶段企业数量 |

### 请求格式
```http
GET /api/dashboard/funnel
Authorization: Bearer <token>
```

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "stage": "POTENTIAL", "name": "潜在企业", "count": 5, "color": "#94a3b8" },
    { "stage": "NO_DEMAND", "name": "无明确需求", "count": 0, "color": "#fbbf24" },
    { "stage": "NO_INTENTION", "name": "没有合作意向", "count": 0, "color": "#ef4444" },
    { "stage": "HAS_DEMAND", "name": "有明确需求", "count": 0, "color": "#3b82f6" },
    { "stage": "SIGNED", "name": "已签约", "count": 0, "color": "#8b5cf6" },
    { "stage": "SETTLED", "name": "已入驻", "count": 0, "color": "#10b981" },
    { "stage": "INCUBATING", "name": "重点孵化", "count": 0, "color": "#f97316" }
  ]
}
```

### 测试用例

| 测试场景 | 请求 | 预期响应 | 实际结果 | 状态 |
|---------|------|---------|---------|------|
| 正常获取漏斗分布 | GET /api/dashboard/funnel | code=200, 返回7个阶段 | 返回7个阶段数据 | ✅ |
| 验证阶段顺序 | - | 按固定顺序返回 | 顺序正确 | ✅ |
| 验证颜色值 | - | 每个阶段有对应颜色 | 颜色值正确 | ✅ |

---

## 6.3 区域分布

### 基本信息
| 项 | 值 |
|-----|-----|
| 路径 | `GET /api/dashboard/districts` |
| 认证 | 🔒 需要Token |
| 说明 | 获取各区域企业数量分布 |

### 请求格式
```http
GET /api/dashboard/districts
Authorization: Bearer <token>
```

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "name": "Xinbei", "count": 2 },
    { "name": "待填写", "count": 2 },
    { "name": "Wujin", "count": 1 }
  ]
}
```

### 测试用例

| 测试场景 | 请求 | 预期响应 | 实际结果 | 状态 |
|---------|------|---------|---------|------|
| 正常获取区域分布 | GET /api/dashboard/districts | code=200, 返回区域列表 | 返回3个区域 | ✅ |
| 验证排序 | - | 按数量降序排列 | 排序正确 | ✅ |

---

## 6.4 行业分布

### 基本信息
| 项 | 值 |
|-----|-----|
| 路径 | `GET /api/dashboard/industries` |
| 认证 | 🔒 需要Token |
| 说明 | 获取各行业企业数量及占比 |

### 请求格式
```http
GET /api/dashboard/industries
Authorization: Bearer <token>
```

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "name": "未分类", "count": 5, "percentage": 100.0 }
  ]
}
```

### 测试用例

| 测试场景 | 请求 | 预期响应 | 实际结果 | 状态 |
|---------|------|---------|---------|------|
| 正常获取行业分布 | GET /api/dashboard/industries | code=200, 返回行业列表 | 返回行业数据 | ✅ |
| 验证百分比计算 | - | 百分比总和为100% | 计算正确 | ✅ |

---

## 6.5 待跟进提醒

### 基本信息
| 项 | 值 |
|-----|-----|
| 路径 | `GET /api/dashboard/pending-follow-ups` |
| 认证 | 🔒 需要Token |
| 说明 | 获取超过30天未跟进、本周需回访企业 |

### 请求格式
```http
GET /api/dashboard/pending-follow-ups
Authorization: Bearer <token>
```

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "overdue30Days": 4,
    "needFollowThisWeek": 0,
    "overdueList": [
      { "id": 5, "name": "企业名称", "lastFollowUp": null, "days": 999 }
    ],
    "weeklyList": []
  }
}
```

### 测试用例

| 测试场景 | 请求 | 预期响应 | 实际结果 | 状态 |
|---------|------|---------|---------|------|
| 正常获取待跟进 | GET /api/dashboard/pending-follow-ups | code=200, 返回待跟进数据 | 返回4家超期企业 | ✅ |
| 验证超期天数计算 | - | 从未跟进显示999天 | 计算正确 | ✅ |
| 验证列表排序 | - | 按超期天数降序 | 排序正确 | ✅ |

---

## 边际测试

| 测试场景 | 请求 | 预期响应 | 实际结果 | 状态 |
|---------|------|---------|---------|------|
| 无Token访问overview | 不带Authorization | code=401 | 返回401 | ✅ |
| 无Token访问funnel | 不带Authorization | code=401 | 返回401 | ✅ |
| 无Token访问districts | 不带Authorization | code=401 | 返回401 | ✅ |
| 无Token访问industries | 不带Authorization | code=401 | 返回401 | ✅ |
| 无Token访问pending-follow-ups | 不带Authorization | code=401 | 返回401 | ✅ |

---

## 测试总结

| API | 状态 | 备注 |
|-----|------|------|
| 6.1 统计概览 | ✅ 通过 | 正确统计各阶段企业数量和本月变化 |
| 6.2 漏斗阶段分布 | ✅ 通过 | 返回7个阶段，顺序和颜色正确 |
| 6.3 区域分布 | ✅ 通过 | 按数量降序排列 |
| 6.4 行业分布 | ✅ 通过 | 正确计算百分比 |
| 6.5 待跟进提醒 | ✅ 通过 | 正确识别超期企业 |

**测试通过率：5/5 = 100%**
