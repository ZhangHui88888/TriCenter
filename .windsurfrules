# Windsurf Rules

## 一、基本行为准则

1. 中文回复，保持一致的中文交流
2. 简洁完成，任务完成后删除多余的测试文件，不生成多余文件
3. 智能编辑，优先搜索替换，避免重写整个文件
4. 分段处理，长文件分段创建/编辑，大文件分段读取
5. 先了解再动手，编辑前先获取相关代码上下文
6. 失败不重试，分析失败原因后再行动
7. 文档管理遵循「三、文档管理规范」，所有文档必须归档到 docs/ 对应子目录并登记到 docs/README.md
8. 功能修改后及时更新 docs/ 中对应文档
9. 禁止执行 git add/commit，由我手动操作
10. 修复控制台警告，运行时不应出现警告
11. 禁止编写未用到的代码，完成后清理无用代码
12. 长耗时命令开新终端窗口，避免阻塞会话
13. 在编写代码时，要避免单个文件过长，合理拆分文件
14. **React 组件文件上限 300 行**：超过时必须立即拆分。复杂页面（含多 Tab/Modal）按 `tabs/` `modals/` `constants.ts` `utils.ts` 目录结构组织，每个 Tab/Modal 独立文件，禁止往主组件追加 JSX
15. 当文件超过 200 行时，主动评估是否需要拆分，不等失控再重构


## 二、工作流

五阶段：**研究** → **创新**（多方案评估）→ **计划**（编号检查清单）→ **执行**（严格按计划）→ **审查**（逐行比对）
每个阶段完成前不跳到下一阶段。


## 三、文档管理规范

### 目录结构

所有项目文档统一存放在 `docs/` 目录下，按类别归入对应子目录：

```
docs/
├── README.md          # 文档导航索引（必须维护）
├── plan/              # 项目管理：里程碑、模块级任务清单
├── tech/              # 技术文档：架构、API设计、数据库、部署、运维
├── sql/               # 数据库脚本：建表与初始化数据（按序号命名）
└── test/              # 测试记录（按模块组织）
```

### 核心规则

1. **新建文档必须登记** — 每次在 `docs/` 下新建或移动文档后，必须同步更新 `docs/README.md`，在对应分类表格中添加一行，包含文件链接和简要说明
2. **归入正确子目录** — 根据文档性质放入对应子目录，禁止直接散放在 `docs/` 根目录
3. **README.md 格式** — 每个分类使用 Markdown 表格（| 文档 | 说明 |），保持链接路径正确
4. **SQL 脚本命名** — 使用数字前缀保证执行顺序（如 `01_`、`02_`），按职责拆分，不合并
5. **删除文档同步清理** — 删除任何文档后，必须从 `docs/README.md` 中移除对应条目
6. **功能变更同步更新** — 功能代码修改后，及时更新 `tech/` 中相关文档
7. **AGENTS.md 体积控制** — `AGENTS.md` 总量 ≤ 2,500 Token；超出时将详细内容拆分到 `docs/` 对应子目录，`AGENTS.md` 只保留摘要和链接

### 去重规则

- **AGENTS.md 不重复 tech/** — 架构、API、数据库等详细内容只在 `docs/tech/` 中维护，`AGENTS.md` 只写摘要和链接
- **plan/ 不含技术栈** — 技术选型在 `docs/tech/技术架构文档.md`，开发计划中不重复
- **plan/ 只记模块级** — 逐接口/逐任务的进度在 `.ai-context.md`，开发计划只跟踪模块级里程碑
- **部署与运维分离** — 部署指南只写部署步骤，日常运维命令在运维手册中，不互相复制


## 四、测试规范

- 每个 API 开发完成后必须进行功能测试，覆盖正常流程和异常情况
- 测试结果按模块记录在 `docs/test/` 目录下
- 测试文档格式：认证要求 → 前置条件 → 请求/响应格式 → 测试用例表（场景、入参、预期、实际、状态）


## 五、安全约束

- **禁止提交**：API 密钥/Secret、.env 真实密码、第三方服务密钥、真实用户隐私数据
- **数据安全**：密码等敏感信息加密存储，使用安全存储方案存储 token，不在日志中输出隐私信息
- **API 安全**：所有请求携带认证 token，敏感接口额外校验，关键操作防重放
- **前端安全**：用户输入校验清理，敏感操作二次确认，Token 过期跳转登录页


## 六、会话记忆协议

### 【强制】每次回答前的第一个动作

1. 读取 `AGENTS.md` 获取项目上下文
2. 读取 `.ai-context.md` 获取当前任务进度（热状态）
3. 读取 `.ai-learnings.md` 获取项目经验（冷存储）
4. 如需了解详细架构/API/进度，读取 `docs/` 对应文档（参考 `docs/README.md` 导航）
5. 未完成以上步骤前，禁止回答任何问题
6. 读取后，用 2-3 行确认当前状态

### 用户触发保存时

当用户说 **"保存" / "checkpoint" / "暂停" / "存一下"** 时，必须：
1. 将当前任务状态写入 `.ai-context.md`（**覆盖**，不追加）
2. 格式：当前任务 / 已完成的步骤 / 下一步计划 / 阻塞问题（如有）
3. 控制在 500 Token 以内，只保留可恢复状态，删除过程细节
4. 同时用 `create_memory` 工具保存（双重保险），Title 格式为 `[会话进度] 简述任务`
5. 保存触发时，禁止修改 `.ai-learnings.md`
6. 告知用户："✓ 进度已保存"

### 用户触发结束时

当用户说 **"结束" / "总结" / "收工"** 时，必须按顺序执行：
1. 回顾本次会话，提取可复用的经验（仅限：踩坑规避规则、架构决策、编码约定）
2. 将经验追加到 `.ai-learnings.md`（不覆盖历史记录）
3. 经验内容控制在 10 条以内，只保留改变未来行为的规则，淘汰过时条目
4. 压缩 `.ai-context.md` 为最小可恢复摘要
5. 同时用 `create_memory` 更新或创建任务进度记忆（双重保险）
6. 告知用户："✓ 经验已记录，进度已压缩"

### 用户触发前后端审核时

当用户说 **"前后端审核"** 时，必须按顺序执行：
1. 根据前端每个页面的每个字段和按钮，去对照后端的每一个接口，看有接口无缺漏，接口设计是否合理，接口的字段有无缺漏

### 用户触发数据库审核时

当用户说 **"数据库审核"** 时，必须按顺序执行：
1. 根据前端和后端每个页面的每个字段和按钮，去对照数据库的每一个，看字段有无缺漏，表设计是否合理？
