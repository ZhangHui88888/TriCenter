# 模块2: 基础数据/数据字典 API测试记录

> 共8个API | 最后测试：2026-02-03

---

## API列表

| 序号 | API | 说明 | 认证要求 | 状态 |
|------|-----|------|----------|------|
| 2.1 | GET /api/options/{category} | 获取系统选项列表 | 🔒 需认证 | ✅ |
| 2.2 | GET /api/options/industries | 获取行业分类树 | 🔒 需认证 | ✅ |
| 2.3 | GET /api/options/product-categories | 获取产品品类树 | 🔒 需认证 | ✅ |
| 2.4 | GET /api/options/users | 获取用户列表（对接人） | 🔒 需认证 | ✅ |
| 2.5 | GET /api/dictionary/categories | 获取字典分类列表 | 🔒 需认证 | ✅ |
| 2.6 | POST /api/dictionary/{category} | 新增字典选项 | 🔐 需管理员 | ✅ |
| 2.7 | PUT /api/dictionary/{category}/{id} | 更新字典选项 | 🔐 需管理员 | ✅ |
| 2.8 | DELETE /api/dictionary/{category}/{id} | 删除字典选项 | 🔐 需管理员 | ✅ |

---

## 通用前置条件

所有API都需要先登录获取Token：

```
步骤1: POST /api/auth/login
入参: {"username":"admin","password":"admin123"}
出参: {"code":200,"data":{"token":"<TOKEN>","user":{...}}}
提取: data.token 值用于后续请求Header
```

---

## 2.1 GET /api/options/{category} - 获取系统选项列表

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔒 需认证 |
| 前置条件 | 需先调用登录API获取Token |

### 请求格式

```http
GET /api/options/{category}
Authorization: Bearer <TOKEN>
```

**支持的category值:**
| category | 说明 | 预期数量 |
|----------|------|----------|
| stage | 漏斗阶段 | 7 |
| district | 区域 | 7 |
| enterprise_type | 企业类型 | 3 |
| staff_size | 人员规模 | 6 |
| revenue | 营收规模 | 5 |
| source | 企业来源 | 4 |
| follow_type | 跟进类型 | 4 |
| trade_mode | 外贸模式 | 5 |
| certification | 产品认证 | 10 |
| cross_border_platform | 跨境平台 | 18 |

### 测试用例

| 序号 | 测试场景 | 请求路径 | 预期结果 | 实际结果 | 状态 |
|------|----------|----------|----------|----------|------|
| 1 | 获取漏斗阶段 | /api/options/stage | >=7条 | 7条 | ✅ |
| 2 | 获取区域 | /api/options/district | >=7条 | 7条 | ✅ |
| 3 | 获取企业类型 | /api/options/enterprise_type | >=9条 | 9条 | ✅ |
| 4 | 获取人员规模 | /api/options/staff_size | >=6条 | 6条 | ✅ |
| 5 | 获取营收规模 | /api/options/revenue | >=5条 | 5条 | ✅ |
| 6 | 获取企业来源 | /api/options/source | >=4条 | 4条 | ✅ |
| 7 | 获取跟进类型 | /api/options/follow_type | >=4条 | 4条 | ✅ |
| 8 | 获取外贸模式 | /api/options/trade_mode | >=5条 | 5条 | ✅ |
| 9 | 获取产品认证 | /api/options/certification | >=10条 | 10条 | ✅ |
| 10 | 获取跨境平台 | /api/options/cross_border_platform | >=18条 | 18条 | ✅ |
| 11 | 不存在的分类 | /api/options/notexist | 空数组 | 空数组[] | ✅ |
| 12 | 特殊字符分类 | /api/options/test<script> | 安全处理 | 空数组 | ✅ |
| 13 | 无Token访问 | /api/options/stage | 403 | 拒绝访问 | ✅ |

---

## 2.2 GET /api/options/industries - 获取行业分类树

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔒 需认证 |
| 前置条件 | 需先调用登录API获取Token |

### 请求格式

```http
GET /api/options/industries
Authorization: Bearer <TOKEN>
```

### 响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "制造业",
      "level": 1,
      "parentId": 0,
      "children": [...]
    }
  ]
}
```

### 测试用例

| 序号 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|------|----------|----------|----------|------|
| 1 | 正常获取 | 返回树结构数组 | 4个顶级分类 | ✅ |
| 2 | 验证树结构 | children正确嵌套 | 包含children字段 | ✅ |

---

## 2.3 GET /api/options/product-categories - 获取产品品类树

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔒 需认证 |
| 前置条件 | 需先调用登录API获取Token |

### 测试用例

| 序号 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|------|----------|----------|----------|------|
| 1 | 正常获取 | 返回树结构数组 | 20个顶级分类 | ✅ |

---

## 2.4 GET /api/options/users - 获取用户列表（对接人）

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔒 需认证 |
| 前置条件 | 需先调用登录API获取Token |

### 响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "value": 1, "label": "系统管理员" },
    { "value": 2, "label": "张三" }
  ]
}
```

### 测试用例

| 序号 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|------|----------|----------|----------|------|
| 1 | 正常获取 | >=1个用户 | 3个用户 | ✅ |
| 2 | 验证格式 | value为ID, label为姓名 | 格式正确 | ✅ |

---

## 2.5 GET /api/dictionary/categories - 获取字典分类列表

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔒 需认证 |
| 前置条件 | 需先调用登录API获取Token |

### 响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "category": "stage", "label": "漏斗阶段", "count": 7 },
    { "category": "district", "label": "区域", "count": 7 }
  ]
}
```

### 测试用例

| 序号 | 测试场景 | 预期结果 | 实际结果 | 状态 |
|------|----------|----------|----------|------|
| 1 | 正常获取 | 返回分类列表 | 24个分类 | ✅ |

---

## 2.6 POST /api/dictionary/{category} - 新增字典选项

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔐 需管理员 |
| 前置条件 | 需先调用登录API获取admin用户Token |

### 请求格式

```http
POST /api/dictionary/{category}
Authorization: Bearer <TOKEN>
Content-Type: application/json

{
  "value": "string (必填, 选项值，唯一)",
  "label": "string (必填, 显示名称)",
  "color": "string (可选, 颜色如#ff0000)",
  "sortOrder": "number (可选, 排序，默认0)"
}
```

### 测试用例

| 序号 | 测试场景 | 请求入参 | 预期HTTP状态码 | 实际结果 | 状态 |
|------|----------|----------|----------------|----------|------|
| 1 | 正常新增 | `{"value":"test_xxx","label":"测试来源"}` | 200 | 返回新选项 | ✅ |
| 2 | value为空 | `{"value":"","label":"测试"}` | 400 | 参数校验失败 | ✅ |
| 3 | label为空 | `{"value":"test","label":""}` | 400 | 参数校验失败 | ✅ |
| 4 | 重复value | `{"value":"已存在的值","label":"x"}` | 400 | 已存在 | ✅ |
| 5 | 带颜色 | `{"value":"test_color","label":"测试","color":"#ff0000"}` | 200 | 保存颜色 | ✅ |
| 6 | 带排序 | `{"value":"test_sort","label":"测试","sortOrder":99}` | 200 | 保存排序 | ✅ |
| 7 | 无Token | `{"value":"notoken","label":"测试"}` | 403 | 拒绝访问 | ✅ |

---

## 2.7 PUT /api/dictionary/{category}/{id} - 更新字典选项

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔐 需管理员 |
| 前置条件 | 需先调用登录API获取admin用户Token |

### 请求格式

```http
PUT /api/dictionary/{category}/{id}
Authorization: Bearer <TOKEN>
Content-Type: application/json

{
  "label": "string (可选, 新显示名称)",
  "color": "string (可选, 新颜色)",
  "sortOrder": "number (可选, 新排序)",
  "isEnabled": "number (可选, 0禁用/1启用)"
}
```

### 测试用例

| 序号 | 测试场景 | 请求路径 | 请求入参 | 预期HTTP状态码 | 实际结果 | 状态 |
|------|----------|----------|----------|----------------|----------|------|
| 1 | 正常更新label | /api/dictionary/source/{id} | `{"label":"新名称"}` | 200 | 更新成功 | ✅ |
| 2 | 更新颜色 | /api/dictionary/source/{id} | `{"color":"#00ff00"}` | 200 | 更新成功 | ✅ |
| 3 | 禁用选项 | /api/dictionary/source/{id} | `{"isEnabled":0}` | 200 | 禁用成功 | ✅ |
| 4 | ID不存在 | /api/dictionary/source/99999 | `{"label":"x"}` | 404 | 选项不存在 | ✅ |
| 5 | category不匹配 | /api/dictionary/stage/{source的id} | `{"label":"x"}` | 404 | 选项不存在 | ✅ |
| 6 | 负数ID | /api/dictionary/source/-1 | `{"label":"x"}` | 404 | 选项不存在 | ✅ |
| 7 | 无Token | /api/dictionary/source/1 | `{"label":"x"}` | 403 | 拒绝访问 | ✅ |

---

## 2.8 DELETE /api/dictionary/{category}/{id} - 删除字典选项

### 基本信息

| 项 | 值 |
|-----|-----|
| 认证要求 | 🔐 需管理员 |
| 前置条件 | 需先调用登录API获取admin用户Token |

### 请求格式

```http
DELETE /api/dictionary/{category}/{id}
Authorization: Bearer <TOKEN>
```

### 测试用例

| 序号 | 测试场景 | 请求路径 | 预期HTTP状态码 | 实际结果 | 状态 |
|------|----------|----------|----------------|----------|------|
| 1 | 正常删除 | /api/dictionary/source/{id} | 200 | 删除成功 | ✅ |
| 2 | ID不存在 | /api/dictionary/source/99999 | 404 | 选项不存在 | ✅ |
| 3 | 负数ID | /api/dictionary/source/-1 | 404 | 选项不存在 | ✅ |
| 4 | 无Token | /api/dictionary/source/1 | 403 | 拒绝访问 | ✅ |

---

## 测试汇总

| 指标 | 值 |
|------|-----|
| 总测试用例 | 39 |
| 已测试 | 39 |
| 通过 | 39 |
| 失败 | 0 |
| 通过率 | 100% |

---

*文档版本：v1.1 | 更新日期：2026-02-03*
