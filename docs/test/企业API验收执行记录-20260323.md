# 企业 API + 数据库验收执行记录

| 项 | 值 |
|----|-----|
| 执行时间 | 2026-03-23 |
| 后端 | `http://localhost:8081` |
| 认证 | `X-API-Key: booking-to-tricenter-secret-key`（与 `application-dev.yml` 中 `tricenter.api-key` 一致） |
| 数据库 | `localhost:3306` / `tricenter`（与 dev 配置一致） |

## 1. 空体创建（与列表页 `enterpriseApi.create({})` 一致）

- **请求**：`POST /api/enterprises`，body `{}`
- **样例**：企业 `id=7`，名称 `新建企业_1774203923450`
- **MySQL 核对**（`enterprises`）：

| 列 | 值 |
|----|-----|
| `province` | 江苏省 |
| `city` | 常州市 |
| `district` | 待填写 |
| `enterprise_type` | 待确认 |
| `stage` | POTENTIAL |

其余业务列多为 `NULL`，与空创建预期一致。

## 2. 全量主表 PUT + 子资源（与 `EnterpriseUpdateRequest` 全量体 + 联系人/产品/专利接口一致）

- **企业**：`id=6`（先空创建得到，再 `PUT` 全量）
- **请求文件**（本地一次性使用，未纳入 fixtures 主集）：`_tmp_put_enterprise_6.json` 已删除；内容对齐 `verify_enterprise_update.json` 并微调跨境文案类字段以贴近前端选项。
- **子资源**：`PUT /api/enterprises/6/contacts`（`verify_enterprise_contacts.json`）；`POST .../products`、`POST .../patents`（对应 fixtures）
- **HTTP**：上述请求均为 **200**
- **MySQL 抽样**：
  - `enterprises`：`name=API验收全量_1774203845031`，`last_year_revenue=1200.50`，`competition_position=leader`，`using_erp=1`
  - `enterprise_contacts`：2 条，主联系人含 `email`/`wechat`/`remark`
  - `enterprise_products`：`验收产品一号`，`export_ratio=55%`，`profit_margin=12%`
  - `enterprise_patents`：`验收专利` / `ZL2025VERIFY0001`

## 3. 说明

- 本记录证明 **接口层 + 库层** 对上述请求一致；**不等于**「详情页每个按钮都已点过」——前端独有假保存项仍以 [企业字段-前端API库映射.md](./企业字段-前端API库映射.md) §4 为准。
- 测试数据企业 `id=6`、`id=7` 可按需删除或保留为回归样本。
- 已据此结果更新 [企业全字段数据库验收清单.md](./企业全字段数据库验收清单.md)：现 **仅保留「详情编辑落库」一列**；空 POST 样例 `id=7` 仍可在本记录中查阅，**不再单独占验收表列**。
