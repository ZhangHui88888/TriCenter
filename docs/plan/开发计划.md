# TriCenter 开发计划

> 本文件由 AI 在用户说"保存"或"更新开发计划"时同步更新。
> 已完成的任务条目直接清除，不保留 ✅ 标记，只保留未完成的待办项。
> 已完成的工作记录到「已完成里程碑」表格中，作为历史归档。
> 全部完成的模块仅保留一行「— 已完成」标记。

## 已完成功能

### 1. 企业管理基础功能
- 企业列表（分页、筛选、搜索）
- 企业创建/编辑/删除
- 企业详情页（多标签页展示）
- 漏斗阶段管理与变更
- 企业批量导入

### 2. 企业详情页 - 标签页
- **基本信息**：企业信息展示/编辑 + 联系人信息管理
- **产品信息**：产品列表CRUD + 自主品牌 + 核心技术/专利
- **外贸信息**：外贸开关 + 基本信息 + 业绩分析 + 市场/模式/品类变化 + 原因分析
- **跨境电商**：跨境开关 + 平台选择 + 基本信息 + 目标市场占比
- **需求分析**：企业画像维度选择 + 需求匹配 + 自定义需求
- **合作**：三中心合作状态 + 合作项目/顾虑 + 评估评分 + 标杆可能性
- **竞争力与风险**：调研状态 + 行业竞争地位 + 当前面临风险
- **跟进记录**：跟进记录CRUD + 阶段变化展示

### 3. 联系人管理（2026-02-12 修复完善）
- 联系人编辑模态框支持多联系人管理
- 支持添加/删除/设为主要联系人
- 字段与数据库完全对齐：姓名、电话、职位、邮箱、微信、备注、是否主要联系人
- 调用后端 `PUT /enterprises/{id}/contacts` 接口批量更新

### 4. 企业详情导出Excel（2026-02-12 新增）
- 导出按钮位于企业详情页顶部右侧
- 8个Sheet对应8个标签页，完整导出所有字段
- 表格样式正式整洁：主题色标题、斑马纹数据行、分区标题
- 联系人信息包含全部7个字段（姓名、电话、职位、邮箱、微信、备注、是否主要联系人）
- 技术栈：exceljs + file-saver
- 详见：[企业详情-导出Excel功能](./企业详情-导出Excel功能.md)

### 5. 看板与数据分析
- Dashboard 统计概览
- 漏斗分析
- 区域/行业分布

### 6. 系统基础
- 用户认证（JWT）
- 数据字典管理
- 跟进记录管理

### 7. 企业列表导出Excel（2026-03-04 新增）
- 导出弹窗支持“企业列表”和“调研表”双模式
- 列表导出复用 exceljs + file-saver，单 Sheet 表格含 11 列
- 导出当前筛选条件下所有企业

### 8. 联系人信息补全（2026-03-04）
- 基本信息标签页联系人卡片新增邮箱、微信、备注字段展示

### 9. 外贸/跨境/合作/竞争力数据持久化（2026-03-04）
- 后端 Entity/DTO 类型修复（growthReasons、declineReasons、crossBorderPlatforms 改为 Object）
- 前端补全 5 个缺失字段映射（market_changes、mode_changes 等）
- 添加 useEffect 从 API 数据初始化本地状态（替代硬编码 mock 值）
- 13 个编辑 Modal 的 onOk 调用 enterpriseApi.update 持久化

### 10. 合作评估数据持久化（2026-03-04）
- 合作项目 Select：defaultValue 改为 value 读取 enterprise.tricenter_demands + onChange 调 API
- 不合作顾虑 Select：defaultValue 改为 value + onChange 调 API，后端 tricenterConcerns 改为 Object/JSON
- 合作评分 Rate ×6：硬编码改为读取 enterprise.*_cooperation_rating + onChange 调 API
- 标杆企业可能性 Slider：硬编码 75% 改为 enterprise.benchmark_possibility + 拖动保存
- 补充说明 TextArea：硬编码改为 enterprise.additional_notes + 保存按钮调 API
- 行业竞争地位点击选择增加 API 持久化

### 11. 需求分析数据持久化（2026-03-04）
- 后端新增 3 个 JSON 字段：dimensionSelections、removedRequirements、customRequirements
- schema.sql 已同步新字段（开发阶段直接重建表）
- 前端 fetchEnterprise 字段映射 + useEffect 初始化恢复本地状态
- 5 个操作函数增加 saveEnterpriseFields 调用（维度选择/移除需求/恢复需求/添加自定义/删除自定义）

### 12. 数据库脚本修复（2026-03-04）
- schema.sql 加 `DROP TABLE IF EXISTS` 17张表 + `SET FOREIGN_KEY_CHECKS`，支持重复执行
- init.sql 乱码修复：全文 UTF-8 重写（1151行），含 system_options/industry_categories/product_categories/requirements/users
- init.sql 头部加 `TRUNCATE TABLE` 4张配置表防重复执行，users 用 `ON DUPLICATE KEY UPDATE`
- 删除不再需要的迁移脚本 `03_add_demand_analysis_fields.sql`

### 13. 数据导入模板修复（2026-03-04）
- 后端 `SurveyExcelController` 新增 `GET /api/survey-excel/template` 调研模板下载端点
- 前端 `surveyExcelApi.downloadTemplate()` + 导入弹窗改调正确 API
- 修复了之前模板格式与导入接口不匹配的问题

### 14. 操作日志记录（2026-03-04）
- 新增 `operation_logs` 表（schema.sql 表18）
- 后端：Entity + Mapper + Service(异步写入) + Controller(分页查询)
- 自定义 `@OpLog` 注解 + AOP 切面自动记录（操作人/IP/操作类型/对象）
- 已标注：企业新增/编辑/删除/阶段变更/批量导入/调研导入
- 添加 `spring-boot-starter-aop` 依赖 + `@EnableAsync`

### 15. Modal 表单受控改造（2026-03-04）
- 11 个编辑弹窗全部改为 Form 实例受控模式（`form.setFieldsValue` + `form.getFieldsValue`）
- 消除全部 32 处 `defaultValue` 硬编码假数据
- 弹窗打开时从 enterprise 状态读取真实数据初始化
- 保存时收集全部表单字段提交 API
- 新增 `region`/`trade_mode`/`trade_team_mode` 系统选项 API 加载
- 涉及：外贸信息、跨境基本信息、跨境需求、三中心合作、痛点、合作评估、初步评估、补充说明、政策支持、行业竞争、风险

### 16. 批量操作（2026-03-04）
- 后端新增 `BatchDeleteRequest` / `BatchStageChangeRequest` DTO
- `EnterpriseService` 新增 `batchDelete(ids)` + `batchChangeStage(ids, stage, reason, operatorId)`
- `EnterpriseController` 新增 `DELETE /api/enterprises/batch` + `PATCH /api/enterprises/batch/stage`（含 `@OpLog`）
- 前端 `enterpriseApi` 新增 `batchDelete` / `batchChangeStage`
- `EnterpriseList` 表格添加多选（rowSelection）、批量操作栏（批量删除 + 批量变更阶段）
- 批量变更阶段弹窗支持选择目标阶段和填写变更原因
- 清理了预先存在的未使用变量警告（`loading`/`total`/`handleSearch`/`setPageSize`）

## 待开发功能

### 低优先级
- [ ] 数据统计报表导出
- [ ] 系统设置页面
