# {模块名} · API 接口文档

## 通用约定

| 属性 | 值 |
|------|-----|
| Base URL | `{baseUrl}` |
| 认证方式 | {Bearer Token / ...} |
| Content-Type | `application/json` |
| 认证头 | `Authorization: Bearer {token}` |

### 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 错误码

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## {功能点1}

### {METHOD} `{路径}`

**描述**：{描述}

**请求头**：

| Header | 必填 | 说明 |
|--------|------|------|
| Authorization | 是 | Bearer {token} |

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {字段名} | {类型} | 是/否 | {说明} |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

---

## {功能点2}

### {METHOD} `{路径}`

{同上格式}
